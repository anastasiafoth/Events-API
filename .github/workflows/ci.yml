name: CI_events-api

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build docker image
        run: docker build -t events-api:latest .

      - name: Run docker container
        run: docker run -d -p 5000:5000 --name events-api events-api:latest

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: run tests
        run: |
          python -m pytest -v

      - name: stop docker container
        if: always()
        run: docker stop events-api

      - name: remove docker container
        if: always()
        run: docker rm events-api

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build docker image
        run: docker build -t events-api:latest .

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag Docker image
        run: docker tag events-api:latest ${{ secrets.DOCKER_USERNAME }}/events-api:latest

      - name: Push docker image to Docker hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/events-api:latest

      - name: Trigger render redeploy
        run: curl -X POST ${{ secrets.RENDER_REDEPLOY_URL }}

      - name: Wait for render to deploy
        run: sleep 30

      - name: Smoke test
        run: curl --fail ${{ secrets.RENDER_URL }}/api/health


